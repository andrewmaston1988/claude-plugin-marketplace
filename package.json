{
  "name": "claude-plugin-marketplace",
  "version": "0.0.0",
  "private": true,
  "description": "Marketplace for Claude Code plugins. Lint config + dev tooling only — no runtime dependencies.",
  "type": "module",
  "scripts": {
    "lint": "eslint plugins/"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0"
  }
}
