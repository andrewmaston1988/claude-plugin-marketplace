Checkpoint resume hint: STATE handoffs for this directory live in `{dir}`. To resume from a previous session, read the newest `STATE*.md` in that directory by filename date (`YYYYMMDDTHHMMSSZ` after the sid sorts lexicographically — newest wins); if no file matches that pattern, read plain `STATE.md` instead. Read the chosen file to resume that work, or ignore it to start clean.{path_surplus}